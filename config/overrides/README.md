# Local Overrides

Optional `.toml` files in this directory are merged last.

This directory is intended for local or deployment overlays. Do not commit secrets here. Prefer environment variables or a secret manager for token secrets, database passwords, APNs/FCM credentials, and object storage keys.
